name: S&P 500 Trend Following Update

on:
  schedule:
    - cron: '0 22 * * 1-5'  # 매일 오전 7시 KST (월-금)
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  update-trend-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install yfinance pandas numpy

      - name: Fetch trend data
        run: python fetch_trend_data.py
        timeout-minutes: 30

      - name: Commit and push
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add -A
          timestamp=$(date -u +"%Y-%m-%d %H:%M UTC")
          git diff --staged --quiet || git commit -m "📊 트렌드 데이터 업데이트: ${timestamp}"
          git push
